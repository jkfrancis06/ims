<form method="post" action="{{ path('intel_partner_delete', {'id': intel_partner.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ intel_partner.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
